const routes = {
    home: '/',
    login: '/login',
    register: '/register',
    note: '/note',
    tag: '/tag',
    notebooks: '/notebooks',
    notebook: '/notebook',
    recycle: '/recycle',
};

export default routes;
