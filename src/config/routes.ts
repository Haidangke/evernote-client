import { Config } from '.';

const routes: Config = {
    home: '/',
    login: '/login',
    note: '/note',
    tag: '/tag',
    notebook: '/notebook',
    recycle: '/recycle',
};

export default routes;
